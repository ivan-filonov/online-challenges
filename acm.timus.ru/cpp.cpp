#include <iostream>
#include <vector>

using std::string;
template <typename V> using vector = std::vector<V>;

int main() {
#ifdef ONLINE_JUDGE
#else
#endif
  std::cin.sync_with_stdio(false);
  return 0;
}
